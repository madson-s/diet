{
    "Portion": {
      "type": "object",
      "properties": {
        "id": {
          "type": "integer",
          "format": "int64"
        },
        "quantity": {
          "type": "string"
        },
        "mealId": {
          "type": "integer",
          "format": "int64"
        },
        "foodId": {
          "type": "integer",
          "format": "int64"
        },
        "mensureId": {
          "type": "integer",
          "format": "int64"
        },
        "measure": {
          "$ref": "#/components/schemas/Measure"
        },
        "food": {
          "$ref": "#/components/schemas/Food"
        },
        "meal": {
          "$ref": "#/components/schemas/Meal"
        },
        "createdAt": {
          "type": "string",
          "format": "date-time"
        },
        "updatedAt": {
          "type": "string",
          "format": "date-time"
        },
        "deletedAt": {
          "type": "string",
          "format": "date-time",
          "nullable": true
        }
      },
      "required": [
        "id",
        "quantity",
        "mealId",
        "foodId",
        "mensureId",
        "createdAt",
        "updatedAt"
      ]
    }
  }
  